import { CaretRight } from "phosphor-react";
import { Container } from "./styles";

const NextButton = () => {
  return (
    <Container>
      <CaretRight size={32} color="var(--PURPLE)" />
    </Container>
  );
};

export default NextButton;
