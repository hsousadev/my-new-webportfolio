import { CaretLeft } from "phosphor-react";
import { Container } from "./styles";

const NextButton = () => {
  return (
    <Container>
      <CaretLeft size={32} color="var(--PURPLE)" />
    </Container>
  );
};

export default NextButton;
