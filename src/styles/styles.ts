import styled from "styled-components";

export const Container = styled.div`
  --PURPLE: #7b4ae2;
  --DARK: #090E16;

  button {
    cursor: pointer;
    appearance: none;
    outline: none;
    background: transparent;
  }
`;
